server:
  port: 8888
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
management:
  security:
    enabled: false  
spring:
  application:
    name: zhushan-config
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/llitianpeng/simple.git
               